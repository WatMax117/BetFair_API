# NetBet Auth Service - Environment Configuration
# Copy this file to .env and fill in your Betfair credentials.
# NEVER commit .env to version control.

# Betfair API credentials (required)
BETFAIR_APP_KEY=your_app_key_here
BETFAIR_USERNAME=your_betfair_username
BETFAIR_PASSWORD=your_betfair_password

# Certificate: use .p12 (matches Java/betfair-bot) or .crt+.key (PEM)
BETFAIR_CERT_PATH=/certs/client-2048.p12
# Required when using .p12:
CERT_PASSWORD=your_cert_password

# Alternative: .crt + .key (PEM) – required for betfair-rest-client
# BETFAIR_CERT_PATH=/certs/client-2048.crt
# BETFAIR_KEY_PATH=/certs/client-2048.key
# betfair-rest-client also supports: BF_USERNAME, BF_PASSWORD, BF_APP_KEY, BF_CERT_PATH, BF_KEY_PATH

# Keep-alive interval in seconds (default: 1020 = 17 minutes)
# KEEP_ALIVE_INTERVAL_SEC=1020

# Token file path for Java Streaming Service
# TOKEN_FILE_PATH=/data/ssoid

# API server bind address and port
# API_HOST=0.0.0.0
# API_PORT=8080
