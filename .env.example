# Postgres credentials for VPS (docker compose at /opt/netbet).
# Copy to .env and set passwords to match the roles created in Postgres (see db/migrations/001_db_isolation.sql).
# Required for Stage 1: REST client must have these to write impedance to Postgres.
# Runbook: scripts/VPS_APPLY_CREDENTIALS_AND_STAGE1.md

POSTGRES_REST_WRITER_USER=netbet_rest_writer
POSTGRES_REST_WRITER_PASSWORD=

POSTGRES_STREAM_WRITER_USER=netbet_stream_writer
POSTGRES_STREAM_WRITER_PASSWORD=

POSTGRES_ANALYTICS_READER_USER=netbet_analytics_reader
POSTGRES_ANALYTICS_READER_PASSWORD=

# Optional: Sticky pre-match 200 markets (see betfair-rest-client/STICKY_PREMATCH_SPEC.md §7a)
# BF_STICKY_PREMATCH=1
# BF_STICKY_K=200
# BF_STICKY_CATALOGUE_MAX=400
# BF_KICKOFF_BUFFER_SECONDS=60
# BF_MARKET_BOOK_BATCH_SIZE=50
# BF_INTERVAL_SECONDS=900
